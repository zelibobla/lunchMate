const middleware = require('../../src/middlewares/mateMiddleware');
const UserInputError = require('../../src/errors/userInputError');

describe(`MateMiddleware`, () => {
  const errorMessage = 'customErrorText';
  test(`Should throw UserInputError in case of no list or empty list`, async () => {
    
  });
});